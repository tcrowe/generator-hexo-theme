{% extends 'includes/layout.njk' %}

{% block body %}
    <article id="post">
        <h1>{{ page.title }}</h1>
        {#to decide if escapes #}
        {{ page.content }}
    </article>

    {% paginator() %}
{% endblock %}
